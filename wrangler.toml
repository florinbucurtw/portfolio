name = "portfolio-api"
main = "src/worker.js"
compatibility_date = "2024-11-01"

# Bind D1 database (fill in after `wrangler d1 create`)
[[d1_databases]]
binding = "DB"
database_name = "portfolio"
database_id = "REPLACE_WITH_D1_ID"

[triggers]
# Run every minute
crons = ["*/1 * * * *"]
